interface:
  display_name: "Author Agent Work Packet"
  short_description: "Create an executable agent work packet"
  default_prompt: "Use $author-agent-work-packet to create a closed set of bounded, verifiable packets for one approved slice."
