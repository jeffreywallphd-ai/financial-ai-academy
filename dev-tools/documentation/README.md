# Documentation Checks

Run:

```text
python dev-tools/documentation/check_docs.py
```

The check validates relative Markdown links, the context-pack catalog, canonical source paths, adjacent-pack identities, and the 200-line pack budget.

