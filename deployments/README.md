# Deployment Profiles

- `local/`: reproducible local open-source composition
- `cloud/`: provider-neutral managed-cloud composition and qualification

Both profiles use the same domain/application artifacts and contract versions.

