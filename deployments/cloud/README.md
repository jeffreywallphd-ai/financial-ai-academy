# Cloud Deployment

The cloud profile remains infrastructure-provider neutral until an ADR selects a platform and deployment tooling.

Future templates must preserve explicit tenant context, managed PostgreSQL/object storage, identity integration, scalable jobs, observability, backup/recovery, and shared-core parity.

