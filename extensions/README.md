# Extensions

This directory is reserved for independently installable provider implementations that need separate versioning, dependencies, contribution, or execution isolation.

Start first-party adapters inside the owning backend module. An extension depends only on public SDKs and executable contracts, never backend internals.

