# Public SDKs

Public SDKs will support provider authors and generated API clients.

- `python/`: provider SDK and generated Python bindings
- `typescript/`: generated API client and public TypeScript bindings

SDKs expose stable public contracts only. They do not re-export backend internals.

